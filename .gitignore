# Throwaway, local Haiku build output dirs (one per configured arch).
# These are produced by running the captive haiku's configure from here;
# see README.md.
/generated*

# macOS noise
.DS_Store
*.dSYM

# Host test build output (produced by tests/Makefile)
/tests/jr_tests
/tests/jr_tests_san
